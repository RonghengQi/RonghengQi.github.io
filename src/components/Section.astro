---
import LocalizedText from "./LocalizedText.astro";

const { id, title, titleZh = title } = Astro.props;
---

<section id={id}>
  <h2><LocalizedText en={title} zh={titleZh} /></h2>
  <hr />
  <slot />
</section>
